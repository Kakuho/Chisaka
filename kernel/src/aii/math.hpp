#pragma once

// Mathematical Capabilities

#include <cstdint>

namespace Aii{

template<typename T>
constexpr T CeilingDivide(T a, T b){
  // calculates Ceil(a / b)
}

template<typename T>
constexpr T Power(T a, T b){
  // calculate a to pow b
}

template<typename T>
constexpr T HiPow2(T a){
  // calculate a to pow b
}

template<typename T>
constexpr T LoPow2(T a){
  // calculate a to pow b
}

} // namespace Aii
